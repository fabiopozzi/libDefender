LibDefender
==========

Libdefender e' una libreria dinamica basata su libSafe e libUnwind.
E' stata scritta con l'obiettivo di impedire l'esecuzione di attacchi basati sulla deviazione del flusso di
esecuzione tramite corruzione del record di attivazione sullo stack (return address o frame pointer).

